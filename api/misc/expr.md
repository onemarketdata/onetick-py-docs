# otp.expr

### ``class Expr(operation)``

Bases: ``object``

EP parameter’s value can be set to an expression.
Expressions are evaluated before parameters are actually passed to event processors.

##### SEE ALSO
``onetick.py.Operation.expr``
